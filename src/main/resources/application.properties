spring.application.name=${SPRING_APPLICATION_NAME:backend}
server.port=${SERVER_PORT:8080}

# PostgreSQL Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.lpoaptxjywmkrpozxsaa.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Restaurant_Dinnerly}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL:true}

# JWT Configuration
jwt.secret=${JWT_SECRET:CXvOSn4wGDVsa7hrkfl9hFbdClxfzBOzKo0ZC62JWpWJhhjuvl501ui923WUMB9GKvuJgZzWmFNCypefHir11w==}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration (Gmail)
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_REQUIRED:true}

# Twilio Configuration for SMS
twilio.account-sid=${TWILIO_ACCOUNT_SID:ACcab3e21dc860968e07d1808606b098d5}
twilio.auth-token=${TWILIO_AUTH_TOKEN:154b97d5305cce14b703b7fe6d9a64fd}
twilio.phone-number=${TWILIO_PHONE_NUMBER:+16405002854}

# Logging
logging.level.root=${LOGGING_LEVEL_ROOT:INFO}
logging.level.com.restaurant.waitlist.backend=${LOGGING_LEVEL_COM_RESTAURANT_WAITLIST_BACKEND:DEBUG}

# Jackson Configuration
spring.jackson.default-property-inclusion=${SPRING_JACKSON_DEFAULT_PROPERTY_INCLUSION:non_null}